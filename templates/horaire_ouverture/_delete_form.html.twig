<form method="post" action="{{ path('app_horaire_ouverture_delete', {'id': horaire_ouverture.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ horaire_ouverture.id) }}">
    <button class="btn">Delete</button>
</form>
